public class Main {
    public static void main(String[] args) {
        int a = 2; // Пример значения переменной a
        int i = (a == 1) ? 1 : (a == 2) ? 2 : 3; // Используем вложенный оператор ? :
        System.out.println("Значение i: " + i); // Проверяем результат
    }
}
